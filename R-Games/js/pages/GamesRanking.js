const GamesRanking = () => {
  // Esta é apenas uma versão simplificada do ranking que poderia ser expandida
  return (
    <a href="#" className="text-blue-600 hover:text-blue-800 transition duration-300 flex items-center">
      Ver ranking completo <i className="fas fa-arrow-right ml-2"></i>
    </a>
  );
};
