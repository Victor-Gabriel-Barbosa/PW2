import { Monda, Monoton, Press_Start_2P } from "next/font/google";

export const monda = Monda({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-monda",
});

export const ps2p = Press_Start_2P({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-ps2p",
});

export const monoton = Monoton({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-monoton",
});